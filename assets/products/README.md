# Product image uploads

Store product photos in this folder and reference them in `products.js`.

Suggested structure:

- `assets/products/women/<image-file>.PNG`
- `assets/products/girl-child/<image-file>.PNG`

Current setup in this project:

- `women/` contains women outfits.
- `girl-child/` contains kids/girl-child outfits.

Guidelines:

- Prefer `.webp` format.
- Recommended width: `1200` to `1600` pixels.
- Keep each file ideally under `400 KB` for fast loading.
- Use descriptive alt-ready names where possible.
